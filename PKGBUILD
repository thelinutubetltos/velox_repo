pkgname=velox-wallpapers
pkgver=1.0
pkgrel=1
pkgdesc="Official Velox Linux wallpapers"
arch=('any')
url="https://github.com/thelinutubetltos/velox-iso"
license=('custom')
source=()

package() {
    mkdir -p "$pkgdir/usr/share/wallpapers/Velox"
    cp "$startdir/wallpapers/"*.png "$pkgdir/usr/share/wallpapers/Velox/"
}
